({
	doInit : function(component, event, helper) {
		/*
		 * var layout = component.get('v.layout');
        var recordObject = component.get('v.recordObject');
        var objectInfo = component.get('v.objectInfo');
        var actualvalues = [];
		var componentdefs = [];        
        layout.sections.forEach(function(section){
            section.layoutRows.forEach(function(layoutRow){                
                layoutRow.layoutItems.forEach(function(layoutItem){
                    layoutItem.layoutComponents.forEach(function(layoutComponent){
                        console.log(objectInfo.fields[layoutComponent.value].dataType,layoutComponent.value);
                        layoutComponent.fieldDef = objectInfo.fields[layoutComponent.value];
                    })
                });
            });  
        });
        component.set('v.layout',layout); 
        */
    }
})